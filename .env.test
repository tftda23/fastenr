# Test Environment Variables
# This file is safe to commit as it contains no real credentials

# Required for testing (use real test project or disable DB tests)
# Option 1: Use a real Supabase test project URL here
# Option 2: Set SKIP_SERVER_TESTS=true to disable database/API tests
NEXT_PUBLIC_SUPABASE_URL=https://your-test-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_test_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_test_service_role_key

# App URLs for testing
NEXT_PUBLIC_APP_URL=http://localhost:3000
APP_BASE_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=test_secret_for_testing_purposes_only_min_32_chars

# Optional services (mock values for testing)
RESEND_API_KEY=re_test_key_for_testing_purposes_only
EMAIL_FROM_SURVEYS=surveys@test.fastenr.com
EMAIL_FROM_NOTIFICATIONS=notifications@test.fastenr.com
EMAIL_FROM_NAME=Fastenr Test Team

# Stripe test keys (safe to commit)
STRIPE_SECRET_KEY=sk_test_mock_key_for_testing_purposes_only
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_mock_key_for_testing_purposes_only
STRIPE_WEBHOOK_SECRET=whsec_test_webhook_secret_for_testing_purposes_only

# Integration test keys (mock)
HUBSPOT_CLIENT_ID=test_hubspot_client_id
HUBSPOT_CLIENT_SECRET=test_hubspot_client_secret
HUBSPOT_REDIRECT_URI=http://localhost:3000/api/integrations/hubspot/callback

SALESFORCE_CLIENT_ID=test_salesforce_client_id
SALESFORCE_CLIENT_SECRET=test_salesforce_client_secret
SALESFORCE_REDIRECT_URI=http://localhost:3000/api/integrations/salesforce/callback

# Test environment flag
NODE_ENV=test

# Skip server-dependent tests (database, API, security) until test DB is configured
SKIP_SERVER_TESTS=true